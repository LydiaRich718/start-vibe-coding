Save the three Claude Code screenshots here with these exact names:

  vscode-download.png   the VS Code download page (Windows / macOS / Linux cards)
  claude-input.png      the Claude Code message box showing the Auto mode pill
  claude-modes.png      the Modes menu (Manual / Edit automatically / Plan / Auto)

Until a file exists the page hides that figure, so nothing looks broken.
